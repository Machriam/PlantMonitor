services:
  webssh:
    build: https://github.com/huashengdun/webssh.git
    ports:
      - "8888"
  gatewayserver:
    build: .
    ports:
      - "8000"
  reverse-proxy:
    image: nginx
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
      - c://srv/secrets:/srv/secrets:ro
    ports:
      - "443:443"
      - "450:450"
      - "80:80"

  
